
<section class="benefits" id="benefits">
  <div class="container">
    <h2 class="section-title">Host the Event Everyone Remembers</h2>
    <p class="section-sub">Planning a party is enough work on its own. Our booth sets up fast, fits any space, and keeps guests smiling all night — so you can just enjoy the day.</p>
    <div class="benefits-grid">
      <div class="benefit-card">
        <div class="benefit-icon" style="background: #f5f0e0;">&#x1F4E6;</div>
        <h3>Sleek & Portable</h3>
        <p>Fits beautifully into any venue, big or small.</p>
      </div>
      <div class="benefit-card">
        <div class="benefit-icon" style="background: #f5f0e0;">&#x1F4F1;</div>
        <h3>Instant Digital Delivery</h3>
        <p>Guests get their photos by text or email, right on the spot.</p>
      </div>
      <div class="benefit-card">
        <div class="benefit-icon" style="background: #f5f0e0;">&#x1F4A1;</div>
        <h3>Studio-Quality Prints & Shots</h3>
        <p>Professional lighting so every guest looks their best.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .benefits { padding: 100px 0; }
  .benefits-grid {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: 32px;
    margin-top: 56px;
  }
  .benefit-card {
    background: var(--sand); border-radius: 16px; padding: 32px 28px;
    border: 1px solid #e8e4df;
    transition: transform .2s, box-shadow .2s;
  }
  .benefit-card:hover { transform: translateY(-4px); box-shadow: 0 12px 40px rgba(0,0,0,.06); }
  .benefit-icon {
    width: 48px; height: 48px; border-radius: 12px;
    display: flex; align-items: center; justify-content: center;
    font-size: 1.4rem; margin-bottom: 20px;
  }
  .benefit-card h3 { font-size: 1.2rem; font-weight: 700; color: var(--ink); margin-bottom: 10px; }
  .benefit-card p { color: var(--slate); font-size: 0.95rem; line-height: 1.7; }

  @media (max-width: 768px) {
    .benefits-grid { grid-template-columns: 1fr; }
  }
</style>
